# Azure Modules

> Placeholder for future Azure infrastructure modules.

## Planned Modules

- `aks/` - Azure Kubernetes Service cluster
- `storage/` - Azure Storage Account with sensitive data
- `identity/` - Managed Identity for AKS workloads

## Usage

When implemented, modules will follow the same patterns as `modules/aws/`.

