# GCP Modules

> Placeholder for future GCP infrastructure modules.

## Planned Modules

- `gke/` - Google Kubernetes Engine cluster
- `storage/` - Cloud Storage bucket with sensitive data
- `identity/` - Workload Identity for GKE workloads

## Usage

When implemented, modules will follow the same patterns as `modules/aws/`.
