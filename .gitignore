# System
.DS_Store
.checkov.yaml

# Terraform
.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl
*.tfvars
!common.auto.tfvars
*.tfplan
# Note: backend.tf is NOT ignored - it must be committed for Wiz Code-to-Cloud IaC mapping
# The partial backend config (no secrets) is safe to commit; actual values come from:
#   - iac_config.wiz (committed) - provides bucket/region/key for Wiz to resolve
#   - backend.hcl (gitignored) - provides values at terraform init time
backend.hcl

# Mise/fnox (local config)
mise.local.toml
fnox.toml
kubeconfig

# Secrets & Sensitive Files
*.secrets.json
secrets.env
*.pem
*.key
*.p12
*.pfx

# Node.js
node_modules/
npm-debug.log
yarn-error.log
.next/
out/
build/
dist/

# Next.js
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Editor
.vscode/
.idea/
*.swp

# wizcli
wizcli

# Agent CLIs (local-only)
AGENTS.md
CLAUDE.md
GEMINI.md

# Binary/Archive files (should not be in repo)
*.zip
*.tar.gz
*.pdf

# Legacy paths (no longer exist, kept for safety)
infra/
